// TODO: Add custom rendering capabilities.
export class Component {
    type;
    config;
    key = Symbol();
    constructor(type, config) {
        this.type = type;
        this.config = config;
    }
    ;
}
;
;
